# The URL the site will be built for
base_url = "https://rescue-respiration.org"

# The site title and description; used in feeds by default.
title = "Rescue Respiration"
description = "Affordable, open-source Ventilators for Everyone."

# The default language; used in feeds.
default_language = "en"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

# # The site theme to use.
# theme = "juice"

# When set to "true", the generated HTML files are minified.
minify_html = false

# When set to "true", emoji aliases translated to their corresponding
# Unicode emoji equivalent in the rendered Markdown files. (e.g.: :smile: => 😄)
render_emoji = true

# Whether to set rel="nofollow" for all external links
external_links_no_follow = true

# Whether to set rel="noreferrer" for all external links
external_links_no_referrer = true

# Whether smart punctuation is enabled (changing quotes, dashes, dots in their typographic form)
# For example, `...` into `…`, `"quote"` into `“curly”` etc
smart_punctuation = true



[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
logo_name = "Rescue Respiration"
logo_path = "lung-red.svg"
external_nav = [
    { title = "Github", link = "https://github.com/rescue-respiration/", weight=4}
]
